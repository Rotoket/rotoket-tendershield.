# ⚠️ ВРЕМЕННО ОТКЛЮЧЕНА АВТОРИЗАЦИЯ

**Дата:** 2025-01-27  
**Статус:** Временная мера для разработки

---

## 🔓 Что изменено

### Frontend (`frontend/src/App.tsx`):
- Автоматически создается тестовый пользователь, если нет авторизации
- Пропускается экран входа в систему

### Backend (`backend/auth.py`):
- `get_current_user()` - теперь возвращает `None` вместо исключения, если токена нет
- `get_current_active_user()` - автоматически создает тестового пользователя, если токена нет
- `get_optional_user()` - автоматически возвращает тестового пользователя, если токена нет

---

## 👤 Тестовый пользователь

**Email:** `test@tendershield.pro`  
**Пароль:** `test123` (не требуется для входа)  
**Имя:** Тестовый Пользователь  
**Компания:** Тестовая Компания  
**Тариф:** Pro  
**Триал:** 14 дней

---

## ⚠️ ВАЖНО: Перед продакшеном

### Нужно удалить/изменить:

1. **`frontend/src/App.tsx`** (строки ~44-58):
   ```typescript
   // Удалить блок с автоматическим созданием testUser
   ```

2. **`backend/auth.py`**:
   - Восстановить `get_current_user()` - должен выбрасывать исключение при отсутствии токена
   - Восстановить `get_current_active_user()` - должен требовать авторизацию
   - Удалить автоматическое создание тестового пользователя в `get_optional_user()`

3. **Вернуть строгую авторизацию:**
   ```python
   # get_current_user должен быть:
   async def get_current_user(...) -> User:  # не Optional!
       if not token:
           raise credentials_exception
   ```

---

## 📝 Как вернуть авторизацию

1. Удалить блоки с `// ВРЕМЕННО:` и `// TODO: Удалить перед продакшеном!`
2. Восстановить оригинальную логику проверки токенов
3. Убедиться, что все эндпоинты требуют авторизацию (кроме публичных)

---

**ВНИМАНИЕ:** Не коммитить эти изменения в production ветку!

